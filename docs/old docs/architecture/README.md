# Archived Architecture Docs

This folder preserves the previous architecture document set by filename after the architecture cleanup on 2026-04-19.

## Important Note

The prior files were removed before they were relocated, and the `docs/` tree is currently untracked in git in this workspace state. That means the original file contents were not recoverable byte-for-byte from git history here.

To avoid losing the archive structure entirely, the original filenames have been recreated in this folder as archive placeholders.

The maintained current architecture set now lives in:

- `docs/architecture/README.md`
- `docs/architecture/system-architecture.md`
- `docs/architecture/backend-architecture.md`
- `docs/architecture/frontend-architecture.md`
- `docs/architecture/workflow-architecture.md`

If you have another copy of the old docs elsewhere, these placeholders can be replaced with the original archived content later.
